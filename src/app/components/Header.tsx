"use client";

import { theme } from "@/styles/styles";
import styled from "styled-components";

export default function Header() {
  return (
    <header>
      <h1>Recipes</h1>
    </header>
  );
}
